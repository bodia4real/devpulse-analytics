# Server (use 5001+ on macOS – port 5000 is often used by AirPlay)
PORT=5001
NODE_ENV=development

# Database (raw PostgreSQL - use Supabase as host: Dashboard > Settings > Database > Connection string)
DATABASE_URL=
# GitHub OAuth (create app at https://github.com/settings/developers)
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret
GITHUB_CALLBACK_URL=http://localhost:5001/api/auth/github/callback

# # JWT
JWT_SECRET=

# Where to send user after GitHub login (e.g. http://localhost:3001 when you have a frontend)
FRONTEND_URL=

# # Worker
# WORKER_INTERVAL_MINUTES=15
# WORKER_ENABLED=true